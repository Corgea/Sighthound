(
    rules: [
        // Code execution vulnerabilities
        (
            mode: "search",
            patterns: Some(["eval"]),
            finding_type: Some("Dangerous Function"),
            severity: Some("Critical"),
            confidence: Some("High"),
            cwe_id: Some("cwe-95"),
            file_types: Some((
                extensions: Some([".py"]),
                include_patterns: None,
                exclude_patterns: None,
            )),
            id: Some("python-eval-001"),
            name: Some("eval() usage"),
            description: Some("Use of eval() can lead to arbitrary code execution"),
            category: Some("security"),
            pattern: None,
            sources: None,
            sinks: None,
            propagators: None,
            sanitizers: None,
            conditions: None,
            tags: Some(["eval", "security", "dangerous"]),
            message: None,
        ),
        (
            mode: "search",
            patterns: Some(["exec"]),
            finding_type: Some("Code Execution"),
            severity: Some("Critical"),
            confidence: Some("High"),
            cwe_id: Some("cwe-95"),
            file_types: Some((
                extensions: Some([".py"]),
                include_patterns: None,
                exclude_patterns: None,
            )),
            id: Some("python-exec-001"),
            name: Some("exec() usage"),
            description: Some("Use of exec() can lead to arbitrary code execution"),
            category: Some("security"),
            pattern: None,
            sources: None,
            sinks: None,
            propagators: None,
            sanitizers: None,
            conditions: None,
            tags: Some(["exec", "security", "dangerous"]),
            message: None,
        ),
        (
            mode: "search",
            patterns: Some(["os.system", "subprocess.call", "subprocess.run"]),
            finding_type: Some("Command Execution"),
            severity: Some("High"),
            confidence: Some("High"),
            cwe_id: Some("cwe-78"),
            file_types: Some((
                extensions: Some([".py"]),
                include_patterns: None,
                exclude_patterns: None,
            )),
            id: Some("python-cmd-001"),
            name: Some("Command execution"),
            description: Some("Direct command execution can be dangerous"),
            category: Some("security"),
            pattern: None,
            sources: None,
            sinks: None,
            propagators: None,
            sanitizers: None,
            conditions: None,
            tags: Some(["command", "security", "execution"]),
            message: None,
        ),
        (
            mode: "search",
            patterns: Some(["pickle.loads", "pickle.load", "cPickle.loads"]),
            finding_type: Some("Unsafe Deserialization"),
            severity: Some("Critical"),
            confidence: Some("High"),
            cwe_id: Some("cwe-502"),
            file_types: Some((
                extensions: Some([".py"]),
                include_patterns: None,
                exclude_patterns: None,
            )),
            id: Some("python-pickle-001"),
            name: Some("Unsafe pickle usage"),
            description: Some("Pickle deserialization is unsafe with untrusted data"),
            category: Some("security"),
            pattern: None,
            sources: None,
            sinks: None,
            propagators: None,
            sanitizers: None,
            conditions: None,
            tags: Some(["pickle", "deserialization", "unsafe"]),
            message: None,
        ),
    ]
) 