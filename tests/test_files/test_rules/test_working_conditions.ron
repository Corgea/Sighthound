{
    injection_sinks: Some([
        // Test with not_literal condition only
        (
            pattern: "*.execute",
            finding_type: Some("sql_injection"),
            severity: Some("high"),
            confidence: Some("high"),
            conditions: Some([
                (
                    type: "not_literal",
                    pattern: None,
                    name: None,
                    not_in: None,
                    parent_type: None,
                    argument_position: Some(0),
                ),
            ]),
            file_types: Some((
                extensions: [".java"],
                include_patterns: None,
                exclude_patterns: None,
            )),
        ),
        // Test command injection with not_literal
        (
            pattern: "Runtime.exec",
            finding_type: Some("command_injection"),
            severity: Some("high"),
            confidence: Some("high"),
            conditions: Some([
                (
                    type: "not_literal",
                    pattern: None,
                    name: None,
                    not_in: None,
                    parent_type: None,
                    argument_position: Some(0),
                ),
            ]),
            file_types: Some((
                extensions: [".java"],
                include_patterns: None,
                exclude_patterns: None,
            )),
        ),
    ]),
    deserialization: [
        // Test basic deserialization with file type filtering
        (
            pattern: "readObject",
            finding_type: Some("insecure_deserialization"),
            severity: Some("high"),
            confidence: Some("high"),
            conditions: None,
            file_types: Some((
                extensions: [".java"],
                include_patterns: Some(["*Controller*"]),
                exclude_patterns: Some(["*test*", "*Test*"]),
            )),
        ),
    ],
} 