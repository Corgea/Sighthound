{
    injection_sinks: Some([
        // Test basic SQL injection without complex conditions
        (
            pattern: "*.execute",
            finding_type: Some("sql_injection"),
            severity: Some("high"),
            confidence: Some("high"),
            conditions: None,
            file_types: Some((
                extensions: [".java"],
                include_patterns: None,
                exclude_patterns: None,
            )),
        ),
        // Test command injection without complex conditions
        (
            pattern: "Runtime.exec",
            finding_type: Some("command_injection"),
            severity: Some("high"),
            confidence: Some("high"),
            conditions: None,
            file_types: Some((
                extensions: [".java"],
                include_patterns: None,
                exclude_patterns: None,
            )),
        ),
    ]),
    deserialization: [
        // Test basic deserialization without complex conditions
        (
            pattern: "readObject",
            finding_type: Some("insecure_deserialization"),
            severity: Some("high"),
            confidence: Some("high"),
            conditions: None,
            file_types: Some((
                extensions: [".java"],
                include_patterns: None,
                exclude_patterns: None,
            )),
        ),
        // Test ObjectInputStream constructor
        (
            pattern: "new ObjectInputStream",
            finding_type: Some("insecure_deserialization"),
            severity: Some("high"),
            confidence: Some("high"),
            conditions: None,
            file_types: Some((
                extensions: [".java"],
                include_patterns: None,
                exclude_patterns: None,
            )),
        ),
    ],
} 