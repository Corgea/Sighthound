{
    injection_sinks: Some([
        // Test SQL injection with file filtering only
        (
            pattern: "*.execute",
            finding_type: Some("sql_injection"),
            severity: Some("high"),
            confidence: Some("high"),
            conditions: None,
            file_types: Some((
                extensions: [".java"],
                include_patterns: None,
                exclude_patterns: None,
            )),
        ),
        // Test command injection with file filtering only
        (
            pattern: "Runtime.exec",
            finding_type: Some("command_injection"),
            severity: Some("high"),
            confidence: Some("high"),
            conditions: None,
            file_types: Some((
                extensions: [".java"],
                include_patterns: None,
                exclude_patterns: None,
            )),
        ),
    ]),
    deserialization: [
        // Test deserialization with file filtering only
        (
            pattern: "readObject",
            finding_type: Some("insecure_deserialization"),
            severity: Some("high"),
            confidence: Some("high"),
            conditions: None,
            file_types: Some((
                extensions: [".java"],
                include_patterns: None,
                exclude_patterns: None,
            )),
        ),
    ],
} 